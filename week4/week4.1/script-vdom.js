// only thing in virtual DOM id we are storing current state inside global variable needs to be updated
// batch update,: it is nothing but ipdating the vdom after all changes in some duration

// state and component
// we the application developer only needs to update state(State). State is what our app currently looks like
// in an array in an object
// ==================================================
// but components iss hoe the aplication looks
// as a developer we only need to update states
// we should also tell react given a state, how it should interact over it to render things (Components) 